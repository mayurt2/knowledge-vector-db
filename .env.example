# No secrets required for local LanceDB setup.
# Copy this file to .env and fill in values as needed.

# Required by the LLM layer (separate service — not used by this repo)
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: override default ports
# RETRIEVAL_PORT=8501
# EXPLORER_PORT=8502
